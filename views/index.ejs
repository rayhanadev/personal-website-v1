<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>RayhanADev</title>
    
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css" type="text/css">
  </head>
  <body>
    <div class="hidden">
      <h2>About Me</h2>
      <p>Hello, name's <%= data.profile.username %>. I'm a frontend developer who enjoys writing CSS. I live around Dallas and am currently a freshman at Westlake Academy! I often use Repl.it to code and enjoy working with API's on backend to get data.</p>
      <h2>My Stats</h2>
      <h4>Repl.it</h4>
      <p>* I have <%= data.profile.karma %> cycles, <%= data.posts %> posts, and <%= data.comments %> comments.</p>
      <% let string = '' %>
      <% for(i in data.profile.languages) { %>
        <% if(i == 0) { %>
          <% string += "I typically use " + data.profile.languages[i].displayName + ". I also use " %>
        <% } else if(i == data.profile.languages.length - 1) { %>
          <% string += "and " + data.profile.languages[i].displayName %>
        <% } else { %>
          <% string += data.profile.languages[i].displayName + ", " %>
        <% } %>
      <% } %>
      <p>* <%= string %>.</p>
      <h4>GitHub</h4>
      <p>* I have <%= data.github.public_repos %> public repos and <%= data.github.followers %> followers.</p>
      <img class="hiddenimg" src="https://github-readme-stats.vercel.app/api?username=RayhanADev"></img>
    </div>
    <script src="script.js" type="text/javascript"></script>
  </body>
</html>
